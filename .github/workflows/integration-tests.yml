name: Integration Tests

env:
  DOTNET_VERSION: "10.0.x"

on:
  schedule:
    # Runs at 09:00 UTC every day to test collectors
    - cron: "0 9 * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - BinDays.Api/**
      - BinDays.Api.Collectors/**
  pull_request:
    paths:
      - BinDays.Api/**
      - BinDays.Api.Collectors/**

jobs:
  discover_councils:
    name: Discover Council Test Files
    runs-on: ubuntu-latest
    outputs:
      councils_json: ${{ steps.list_councils.outputs.councils_json }}
    env:
      # JSON array of council names to skip during test discovery (e.g. '["CouncilA","CouncilB"]').
      # Empty array means no councils are excluded.
      EXCLUDED_COUNCILS: "[]"
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: List council test file identifiers
        id: list_councils
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_BASE_REF: ${{ github.base_ref }}
        run: ./.github/scripts/integration-tests/discover-councils.sh

  run_council_tests:
    name: Test ${{ matrix.council }}
    needs: discover_councils
    runs-on: self-hosted
    strategy:
      fail-fast: false
      matrix:
        council: ${{ fromJson(needs.discover_councils.outputs.councils_json) }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Setup Dart SDK
        uses: dart-lang/setup-dart@v1

      - name: Workaround for SSL error on self-hosted runner
        run: ./.github/scripts/integration-tests/ssl-workaround.sh

      - name: Run Integration Tests for ${{ matrix.council }}
        uses: nick-invision/retry@v3
        with:
          timeout_minutes: 5
          max_attempts: 2
          command: |
            echo "Running tests for ${{ matrix.council }}..."
            dotnet test --filter "FullyQualifiedName~${{ matrix.council }}Tests" --logger "console;verbosity=detailed" BinDays.Api.IntegrationTests/BinDays.Api.IntegrationTests.csproj
